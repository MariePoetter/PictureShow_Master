


( function( $ ) {
	
	// Setup variables
	$window = $(window);
	$slide = $('.homeSlide');
	$body = $('body');
	
    //FadeIn all sections   
	$body.imagesLoaded( function() {
		setTimeout(function() {
		      
		      // Resize sections
		      adjustWindow();
		      
		      // Fade in sections
			  $body.removeClass('loading').addClass('loaded');
			  
		}, 800);
	});
	
	function adjustWindow(){
		
		// Init Skrollr
		
		
		// Get window size
	    winH = $window.height();
	    
	    // Keep minimum height 550
	    if(winH <= 550) {
			winH = 550;
		} 
	    
	    // Resize our slides
	    $slide.height(winH);
	    
	    // Refresh Skrollr after resizing our sections
	    
	    
	}
	
jQuery(document).ready(function() {         jQuery('#ichbins2').hide();  });     jQuery(document).ready(function() {         jQuery('#zeigen').click(function() {            jQuery('#ichbins2').fadeIn('slow');      });         jQuery('#zeigen').click(function() {            jQuery('#ichbins').fadeOut('slow');         });     });
		
} )( jQuery );